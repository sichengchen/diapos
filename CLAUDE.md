# Claude Code

Read `AGENTS.md` first — it has the project rules, structure, and pointers to the constitution and specs.

## EsperKit

Before starting work, read `.esper/CONSTITUTION.md` and `.esper/WORKFLOW.md`.

Use esper skills (`esper:spec`, `esper:atom`, `esper:go`, etc.) instead of ad-hoc planning. The workflow is: **spec → increment → implement → verify → sync**.
